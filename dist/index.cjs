const {
  startServer,
  stopServer,
  addStaticDirectory,
  addDynamicDirectory,
} = require('../lib/server.zigar');

module.exports = {
  startServer,
  stopServer,
  addStaticDirectory,
  addDynamicDirectory,
};